## Vector

```c++
# 二维 vector 初始化固定大小
int row = matrix.size();
int col = matrix[0].size();
vector<vector<int>> dp(row, vector<int> (col, 0));

# 返回空数组
return vector<int>();

# 返回单个元素/单个初始化元素，例如返回 [0]
return vector<int>(1, 0);

```

